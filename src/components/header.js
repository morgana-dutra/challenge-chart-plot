/* Page's Header with candidate's name*/

import React from 'react';

import './header.css';

const Header = props => (
	<header>
		<div className = "header">
			{"Morgana's Challenge"}
		</div>
	</header>	
);

export default Header;